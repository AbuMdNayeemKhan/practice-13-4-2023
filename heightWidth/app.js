let box = document.querySelector(".box");

let width = box.clientHeight;

let height = box.clientWidth;

let showText = `width is ${width} and height is ${height}`;

console.log(showText);


































